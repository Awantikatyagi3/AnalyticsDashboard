---
version: 3
systemUnderTest:
  service:
    definitions:
    - definition:
        source:
          filesystem:
            directory: "."
        specs:
        - "contracts/api.yaml"
        - "workflow/DenaAnalyticsJourney.arazzo.yaml"
    runOptions:
      openapi:
        actuatorUrl: "http://localhost:{PORT:3000}/actuator/mappings"
